# Secrets
gemini_key.json
*.key
*.pem
config.json
# The main config.json has API keys; never push it.
# Per-seed configs at agentopia_runs/seed*/config.json also out of repo by location.

# Local IDE / tool settings
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# OS
.DS_Store

# Run outputs (large + per-machine)
data/apartment_*/
data/school_*/
token_usage.jsonl
run.log
logs/
*.pid
